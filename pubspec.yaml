name: goti12
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_soloud: ^3.5.4
  cupertino_icons: ^1.0.8
  confetti: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # FIXED: Moved from dependencies down to the root flutter block with exact spacing
  assets:
    - assets/sounds/piece_slide.mp3
    - assets/sounds/piece_captured.mp3 # FIXED: Added the missing 'd' to match your Dart code
    - assets/sounds/tap.mp3